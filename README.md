# tstools

基于 Next.js 的 TypeScript 项目脚手架。

## 开发

```bash
npm install
npm run dev
```

打开 http://localhost:3000 查看页面。

## 构建

```bash
npm run build
```

## 说明

- 使用 Next.js App Router
- 严格 TypeScript（`tsconfig.json` 中 `strict: true`，`allowJs: false`）
- 未引入 Tailwind、ESLint 等额外框架依赖
